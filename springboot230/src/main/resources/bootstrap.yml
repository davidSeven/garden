server:
  port: 8087
  tomcat:
    basedir: ${info.config.tmp}
    max-threads: 2000
    accept-count: 10000
    max-connections: 10000
    min-spare-threads: 100
spring:
  application:
    name: springboot230
