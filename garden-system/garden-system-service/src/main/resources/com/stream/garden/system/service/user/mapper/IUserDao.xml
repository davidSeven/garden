<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.dao.IUserDao">
    <resultMap id="BaseResultMap" type="web.model.User">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="id_card" jdbcType="VARCHAR" property="idCard" />
        <result column="surname" jdbcType="VARCHAR" property="surname" />
        <result column="pinyin" jdbcType="VARCHAR" property="pinyin" />
    </resultMap>
    <sql id="BaseTable">
        area
    </sql>

    <insert id="insert" parameterType="web.model.User" useGeneratedKeys="true" keyProperty="id">
        insert into `user`(id,`name`,password,id_card,surname,pinyin)
        values (#{id,jdbcType=INTEGER},#{name,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR},#{idCard,jdbcType=VARCHAR},#{surname,jdbcType=VARCHAR},#{pinyin,jdbcType=VARCHAR})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        insert into `user`(id,`name`,password,id_card,surname,pinyin) values
        <trim prefix=""  suffixOverrides="," >
            <foreach collection="list" item="model">
              (#{model.id,jdbcType=INTEGER},#{model.name,jdbcType=VARCHAR},#{model.password,jdbcType=VARCHAR},#{model.idCard,jdbcType=VARCHAR},#{model.surname,jdbcType=VARCHAR},#{model.pinyin,jdbcType=VARCHAR}),
            </foreach>
        </trim>
    </insert>

    <update id="update" parameterType="web.model.User">
        update `user`
        <trim prefix="SET"  suffixOverrides="," >
            `name` = #{name,jdbcType=INTEGER},
            password = #{password,jdbcType=VARCHAR}
        </trim>
        where id = #{id,jdbcType=INTEGER}
    </update>

    <select id="get" resultMap="BaseResultMap" parameterType="java.lang.Object" >
        select * from `user`
        where id = #{id,jdbcType=INTEGER}
    </select>

    <select id="list" resultMap="BaseResultMap" parameterType="web.model.User" >
        select * from `user`
        <trim prefix="WHERE"  prefixOverrides="AND | OR" >
            <if test="id != null" >
                AND id = #{id, jdbcType=INTEGER}
            </if>
            <if test="name != null" >
                AND `name` = #{name, jdbcType=VARCHAR}
            </if>
            <if test="surname != null" >
                AND surname = #{surname, jdbcType=VARCHAR}
            </if>
            <if test="password != null" >
                AND password = #{password, jdbcType=VARCHAR}
            </if>
        </trim>
        <include refid="datascope"/>
    </select>

    <select id="pageList" resultMap="BaseResultMap" parameterType="web.model.User" >
        select * from `user`
        <trim prefix="WHERE"  prefixOverrides="AND | OR" >
            <if test="id != null" >
                AND id = #{id, jdbcType=INTEGER}
            </if>
            <if test="name != null" >
                AND `name` = #{name, jdbcType=VARCHAR}
            </if>
            <if test="surname != null" >
                AND surname = #{surname, jdbcType=VARCHAR}
            </if>
            <if test="password != null" >
                AND password = #{password, jdbcType=VARCHAR}
            </if>
        </trim>
        <include refid="datascope"/>
    </select>

    <sql id="datascope">

    </sql>
</mapper>